Running with docker in localhost:
```
docker-compose up
```

and the console message will be:
```
Starting alerta-mercurio_web_1 ... done
Attaching to alerta-mercurio_web_
```

open the browser and enter http://localhost
